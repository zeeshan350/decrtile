import { ElementRef, Renderer2, OnDestroy, AfterViewInit } from '@angular/core';
import { ShowHideService } from './show-hide.service';
export interface ShowHideStatusConfig {
    id: string;
    show?: string;
    hide?: string;
    materialIcon?: boolean;
}
export declare class ShowHideStatusDirective implements AfterViewInit, OnDestroy {
    private service;
    private el;
    private renderer;
    private subscription;
    private config;
    showHideStatus?: ShowHideStatusConfig;
    constructor(service: ShowHideService, el: ElementRef, renderer: Renderer2);
    ngAfterViewInit(): void;
    private updateStatus;
    ngOnDestroy(): void;
}
